databases:
  - name: manage-my-payroll
    databaseName: manage-my-payroll
    user: manage-my-payroll

services:
  - type: web
    name: manage-my-payroll
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: manage-my-payroll
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false